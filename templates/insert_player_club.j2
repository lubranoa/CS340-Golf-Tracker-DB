<!DOCTYPE html>
<html lang="en">
{#  
    Citation for the boilerplate HTML
    Date: 07/18/2022
    Copied from: MUI CSS/JS
    Source URL: https://www.muicss.com/docs/v1/css-js/boilerplate-html 
#}
<head>
    <title>Golf Tracker</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="//cdn.muicss.com/mui-0.10.3/css/mui.min.css" rel="stylesheet" type="text/css" />
    <script src="//cdn.muicss.com/mui-0.10.3/js/mui.min.js"></script>
    <link href="./../static/css/all-templates.css" rel="stylesheet" />
    <link href="./../static/css/insert-forms.css" rel="stylesheet" />
</head>
<body>
<div class="mui-appbar">
    <ul class="mui-list--inline">
        <li><a href="/clubs">View clubs</a></li>
        <li><a href="/courses">View courses</a></li>
        <li><a href="/holes">View holes</a></li>
        <li><a href="/player-clubs">View player_clubs</a></li>
        <li><a href="/players">View players</a></li>
        <li><a href="/rounds">View rounds</a></li>
        <li><a href="/swings">View swings</a></li>
    </ul>
</div>
<div class="app">
    <h1>Insert player_club Entry</h1>
    {#  
        Citation for the form, input, select, and button styles
        Date: 07/27/2022
        Adapted from: MUI CSS/JS
        Source URL: https://www.muicss.com/docs/v1/css-js
    #}
    <form class="mui-form--inline" action="/insert-player-club" method="post">
        <div class="mui-select">
            <label>player:</label>
            <select id="player" name="player" required>
                <option>--Choose player--</option>
                {# Get and place all players into dropdown options #}
                {% for player in gt_players %}
                <option value={{ player["player_id"] }}>{{ player["player_name"] }}</option>
                {% endfor %}
            </select>
        </div>
        <div class="mui-select">
            <select id="club" name="club" required>
                <option>--Choose club--</option>
                {# Get and place all clubs into dropdown options #}
                {% for club in gt_clubs %}
                {# Concatenate club brand, name, and type to display as dropdown options #}
                <option  value={{ club["club_id"] }}>{{ club["brand"] ~ " " ~ club["club_name"] ~ " " ~ club["club_type"] }}</option>
                {% endfor %}
            </select>
            <label>club</label>
        </div>
        <br>
        <button type="submit" class="mui-btn mui-btn--raised mui-btn--primary">insert player_club</button>
    </form>
    <div class="home-link"><a href="/">Go Back to Homepage</a></div>
</div>
</body>
</html>