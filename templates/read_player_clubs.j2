<!DOCTYPE html>
<html lang="en">
{#  
    Citation for boilerplate HTML
    Date: 07/18/2022
    Copied from: MUI CSS/JS
    Source URL: https://www.muicss.com/docs/v1/css-js/boilerplate-html 
#}
<head>
    <title>Golf Tracker</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="//cdn.muicss.com/mui-0.10.3/css/mui.min.css" rel="stylesheet" type="text/css" />
    <script src="//cdn.muicss.com/mui-0.10.3/js/mui.min.js"></script>
    <link href="./../static/css/all-templates.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
</head>
<body>
{% include './nav_bar.j2' %}
<div class="app">
    <h1>The player_clubs Table</h1>
    <h2>Please test our application in Mozilla Firefox</h2>
    {#  
        Citation for the table style
        Date: 07/18/2022
        Copied from: MUI CSS/JS
        Source URL: https://www.muicss.com/docs/v1/css-js/buttons 
    #}
    <table class="mui-table mui-table--bordered">
    <thead>
        <tr>
            {# Get key names and insert as table header column names #}
            {% if gt_player_clubs[0]|length > 0 %}
            {% for key in gt_player_clubs[0].keys() %}
            <th>{{ key }}</th>
            {% endfor %}
            <th>Delete</th>
            {% else %}
            <p>This table is empty.</p>
            <p> Please use button below to insert data.</p>
            {% endif %}
        </tr>
    <tbody>
        {# Get and display all player_clubs data in the table rows #}
        {% for pl_club in gt_player_clubs %}
        <tr>
            {% for key in pl_club.keys() %}
            <td class="table-data">{{ pl_club[key] }}</td>
            {% endfor %}
            <td>
                <a href="/delete-player-club/{{ pl_club["player_id"] }}/{{ pl_club["club_id"] }}">
                    <button class="mui-btn mui-btn--small mui-btn--raised mui-btn--danger">
                        <span class="material-symbols-outlined">delete</span>
                    </button>
                </a>
            </td>
        </tr>
        {% endfor %}
        <tr>
            <td colspan="10">
            <div>
                <a href="/insert-player-club">
                    {#  
                        Citation for the button style
                        Date: 07/27/2022
                        Adapted from: MUI CSS/JS
                        Source URL: https://www.muicss.com/docs/v1/css-js/buttons 
                    #}
                    <button class="mui-btn mui-btn--small mui-btn--primary mui-btn--fab">+</button>
                </a>
            </div>
            </td>
        </tr>
    </table>

    {#  
        Citation for the form style
        Date: 08/05/2022
        Adapted from: MUI CSS/JS Forms
        Source URL: https://www.muicss.com/docs/v1/css-js/forms
    #}
    <table  class="mui-table mui-table--bordered">
            <form action="/player-clubs/search" class="mui-form">
                <tr>
                <td>
                    <label>Search by player name:  </label>
                </td>
                <td>
                    <div class="mui-textfield">
                        <input type="text" id="player_name" name="player_name" placeholder="Player Name" required>
                    </div>
                </td>
                <td>
                    <button type="submit" class="mui-btn mui-btn--primary">Submit</button>
                </td>
                </form>
                <td>
                <a href="/player-clubs" class="mui-form">
                    <button class="mui-btn mui-btn--primary">Clear</button>
                </a>
                </td>
            </tr>
    </table>
    <div class="home-link"><a href="/">Go Back to Homepage</a></div>
</div>
</body>
</html>